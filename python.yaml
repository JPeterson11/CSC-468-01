apiVersion: apps/v1
kind: Deployment
metadata:
 name: worker
spec:
 replicas: 1
 selector:
   matchLabels:
     app: python
 strategy:
   type: Recreate
 template:
   metadata:
     labels:
       app: python
   spec:
     containers:
     - image: jp923870/python
       name: worker
       ports:
           - containerPort: 5000
